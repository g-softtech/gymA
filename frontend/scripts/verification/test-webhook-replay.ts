import { POST } from '../../app/api/webhooks/platform-billing/route'; import crypto from 'crypto'; async function run() { const payload = { event: 'charge.success', data: { reference: 'replay_' + Date.now(), metadata: { tenantId: 'hardening_tenant_1', targetPlan: 'GROWTH' }, customer: { customer_code: 'CUS_REPLAY1' } } }; const bodyStr = JSON.stringify(payload); const secret = process.env.PAYSTACK_SECRET_KEY || 'sk_test_fake'; const hash = crypto.createHmac('sha512', secret).update(bodyStr).digest('hex'); const createReq = () => new Request('http://localhost/api/webhooks/platform-billing', { method: 'POST', headers: { 'Content-Type': 'application/json', 'x-paystack-signature': hash }, body: bodyStr }); const res1 = await POST(createReq()); console.log('Request 1:', res1.status, await res1.text()); const res2 = await POST(createReq()); console.log('Request 2:', res2.status, await res2.text()); } run().catch(console.error);
